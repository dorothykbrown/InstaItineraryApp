<div class="container p-3">
  <div class="search-container">
    <%= simple_form_for :search, url: root_path, method: :get do |f| %>
      <div class="search-form-control form-group">
        <%#= render "shared/search" %>
        <input class="form-control string required" type="text" name="search[query]"
      id="search_query"/>
        <div class="i">
          <i class="fas fa-search"></i>
        </div>
      </div>
    <% end %>
    <%= form_for '' do |f| %>
      <div class="cards">
        <% Category.all.each do |category| %>
          <input type="checkbox" class="d-none" value="<%= category.id %>" name="post[category_ids][]" id="post_category_ids_<%= category.id %>">
          <label class="card-category" style="background-image: url(<%= category.image_url %>);" for="post_category_ids_<%= category.id %>">
            <p><%= category.name %></p>
          </label>
        <% end %>
      </div>
      <%= f.submit "Submit Categories", class: "btn btn-flat"  %>
    <% end %>
  </div>
</div>